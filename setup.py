from setuptools import setup

setup(
    name='websleydale',
    version='2.0-alpha1',
    install_requires=[
        'Pygments >=1.6',
        'docutils >=0.11',
    ],
    packages=['websleydale'],
)
